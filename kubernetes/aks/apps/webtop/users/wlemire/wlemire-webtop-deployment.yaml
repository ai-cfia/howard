apiVersion: apps/v1
kind: Deployment
metadata:
  name: wlemire-webtop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wlemire-webtop
  template:
    metadata:
      labels:
        app: wlemire-webtop
    spec:
      containers:
      - name: webtop
        image: ghcr.io/ai-cfia/webtop-ubuntu-kde:main
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: wlemire-webtop-secrets
        volumeMounts:
        - name: config
          mountPath: /config
      volumes:
      - name: config
        persistentVolumeClaim:
          claimName: wlemire-webtop-config-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: wlemire-webtop-service
spec:
  type: ClusterIP
  selector:
    app: webtop
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wlemire-webtop-config-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
